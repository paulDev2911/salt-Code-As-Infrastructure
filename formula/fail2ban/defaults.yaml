fail2ban:
  loglevel: INFO
  logtarget: /var/log/fail2ban.log
  bantime: 600
  findtime: 600
  maxretry: 5
  backend: auto
  usedns: warn
  jails:
    sshd:
      enabled: true
      port: ssh
      filter: sshd
      logpath: /var/log/auth.log
      maxretry: 3
